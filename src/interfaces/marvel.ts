import { DataObj } from './dataObj';

export type Marvel = {
  attributionHTML: string;
  attributionText: string;
  code: number;
  copyright: string;
  data: DataObj;
  etag: string;
  status: string;
};
